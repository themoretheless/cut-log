[package]
name = "cutter-ui"
version = "0.1.1"
edition = "2021"
description = "Frontend helpers: SVG rendering, Three.js scene JSON, colors"

[dependencies]
cutter-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
